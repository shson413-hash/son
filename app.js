const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];function pick(a){return a[Math.floor(Math.random()*a.length)]}function toast(t='복사 완료!'){let e=$('.toast');if(!e){e=document.createElement('div');e.className='toast';document.body.appendChild(e)}e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),1500)}async function shareResult(title,text){const data={title,text,url:location.href};try{if(navigator.share){await navigator.share(data)}else{await navigator.clipboard.writeText(`${title}\n${text}\n${location.href}`);toast('결과 링크 복사 완료 ✨')}}catch(e){}}function copyResult(text){navigator.clipboard.writeText(`${text}\n${location.href}`);toast('복사 완료 ✨')}function randPercent(min=41,max=99){return Math.floor(Math.random()*(max-min+1))+min}function showResult(el){el.classList.remove('show');void el.offsetWidth;el.classList.add('show')}document.addEventListener('DOMContentLoaded',()=>{$$('.share-btn').forEach(b=>b.addEventListener('click',()=>shareResult(document.title,b.dataset.share||document.body.innerText.slice(0,120))));});